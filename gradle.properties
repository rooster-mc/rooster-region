# Keeps Kotlin stdlib out of the published rooster-region POM; stdlib is a
# compileOnly dependency that consumers already provide at runtime.
kotlin.stdlib.default.dependency=false
org.gradle.jvmargs=-Xmx2g
